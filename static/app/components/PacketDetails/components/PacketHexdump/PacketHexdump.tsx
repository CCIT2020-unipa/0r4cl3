import * as React from 'react'
import { IPacketWithPayload } from '../../../../net/api'

export const PacketHexdump: React.SFC<IProps> = ({ packet }) => (
  null
)

interface IProps {
  packet: IPacketWithPayload
}
